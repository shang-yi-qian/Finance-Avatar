// Phase 1: typed wrappers around Convex queries/mutations
// After running `npx convex dev`, import from convex/_generated/api
// Example usage once generated:
//   import { api } from "../../convex/_generated/api";
//   const user = useQuery(api.users.getUser, { userId: "kai_demo" });
//   const savePitch = useMutation(api.pitches.savePitch);

export {};
